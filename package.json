{
  "name": "aglc4-mcp",
  "version": "0.1.0",
  "description": "MCP server that indexes and searches the AGLC4 guide PDF.",
  "type": "module",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "index": "tsx scripts/build-index.ts",
    "search": "tsx scripts/quick-search.ts",
    "health": "tsx scripts/health.ts",
    "lint": "eslint .",
    "format": "prettier -w .",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "eslint": "^9.10.0",
    "prettier": "^3.3.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2",
    "vitest": "^2.0.5"
  }
}
